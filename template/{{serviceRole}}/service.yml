type: {{serviceType}}
description: {{description}}
author: {{author}}

setup: glide install
startup:
  command: go run server.go
  online-text: online at port
tests: go test

messages:
  receives:
    - ping
  sends:
    - pong

dependencies:
